pub struct Map {
    width: i32,
    height: i32,
}

/// Represents a position in the map
///
/// It follow cartesian coordinates so the lower left part of the map is 
/// position (0,0)
pub struct Pos {
    x: i32,
    y: i32,
}

