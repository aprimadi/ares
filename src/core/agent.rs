trait Agent {
    // Armor affects damage received
    fn armor() -> f32;
    // Strength affects damage done
    fn strength() -> f32;
    // Speed affects dodge and turn speed
    fn speed() -> f32;
}

